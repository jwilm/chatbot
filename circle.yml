dependencies:
  override:
    - curl -sSf https://static.rust-lang.org/rustup.sh | sh -s -- --channel=nightly -y

test:
  override:
    - cargo test

deployment:
  production:
    branch: master
    commands:
      - sh mkdoc.sh
